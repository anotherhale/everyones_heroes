/// Why the Hero wants to tell this story (session-local Builder vocabulary).
///
/// Distinct from authoritative [StoryClassification] and Discovery narrative themes.
/// Includes [notSureYet] as an intentional selection, not the same as unset (`null`).
enum StoryBuilderPurpose {
  inspireSomeone,
  encourageSomeone,
  helpSomeoneFeelLessAlone,
  preserveAMemory,
  honorSomeone,
  shareALesson,
  helpSomeoneFacingSomethingSimilar,
  simplyTellMyStory,
  notSureYet,
}
