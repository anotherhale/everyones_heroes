name: ai_proxy
description: Everyone's Heroes AI backend proxy for transcription (HS.11) and Story Builder coaching (SB.7).
version: 0.2.0
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  http: ^1.6.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
