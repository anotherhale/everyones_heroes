/// Named laboratory experiment presets.
///
/// Experiment A is the only implemented combination in this slice.
enum ExperienceLabExperiment {
  /// OpenAI creative direction + OpenAI TTS + Stable Audio 3.0 (Strategy A).
  experimentA,
}
