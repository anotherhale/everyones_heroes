/// Typed step kinds in a Story Experience Plan sequence (HS.12.4).
///
/// Defines the plan only — Experience runtime belongs to a later slice.
enum StoryExperienceStepType {
  story,
  keyMoment,
  reflection,
  music,
}
